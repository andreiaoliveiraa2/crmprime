# CSO Clone — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar o projeto CSO (Corretor de Seguros Organizado) como clone do CRM A2 Prime com nova identidade visual marrom/bege/roxo, em repositório e banco separados.

**Architecture:** Cópia completa do código-fonte do CRM A2 Prime em nova pasta `cso-corretor`, sem alterações funcionais. Todas as mudanças são de identidade visual (cores, logo, nome do sistema) e renomeação de seções. Banco Supabase independente configurado via variáveis de ambiente.

**Tech Stack:** Next.js 15, TypeScript, Tailwind CSS, Supabase, EasyPanel

**Diretório do novo projeto:** `C:\Users\Andreia Ferreira\cso-corretor`

> **Nota:** Todos os caminhos de arquivo abaixo são relativos a `C:\Users\Andreia Ferreira\cso-corretor`, não ao repositório original.

---

## Mapeamento de arquivos

| Arquivo | Tipo | O que muda |
|---|---|---|
| `src/app/globals.css` | Modificar | Variáveis de cor (background, brand colors) |
| `public/manifest.json` | Modificar | Nome, cores PWA, ícone |
| `src/components/Sidebar.tsx` | Modificar | Cores inline, logo, "Prime Academy"→"CSO Academy", "A2 Prime"→"CSO" |
| `src/app/layout.tsx` | Modificar | title, description, ícones |
| `src/app/login/page.tsx` | Modificar | Logo e alt text |
| `src/app/(protected)/prime-academy/` | Renomear → `cso-academy/` | Rota e referências internas |
| `src/components/WhatsappPrimeClient.tsx` | Modificar | Import path após renomear pasta |
| `src/components/MpaClient.tsx` | Modificar | Import path após renomear pasta |
| `src/components/ResultadoTab.tsx` | Modificar | Texto "A2 Prime" no PDF |
| `src/components/FinanceiroClient.tsx` | Modificar | Texto "A2 Prime" no cabeçalho |
| `src/components/ProducaoTab.tsx` | Modificar | Texto "A2 Prime" no PDF |
| `src/components/ClienteExportModal.tsx` | Modificar | Texto "A2 Prime" no PDF |
| `public/logo-a2prime.png` | Substituir | Logo CSO (depende de exportação do Canva) |
| `.env.local` | Criar | Variáveis do novo projeto Supabase |

---

## Task 1: Copiar projeto para nova pasta e inicializar git

**Files:**
- Criar: `C:\Users\Andreia Ferreira\cso-corretor\` (nova pasta)

- [ ] **Step 1: Copiar arquivos (excluindo node_modules, .next, .git)**

Execute no PowerShell:
```powershell
robocopy "C:\Users\Andreia Ferreira\crm-seguros" "C:\Users\Andreia Ferreira\cso-corretor" /E /XD ".git" "node_modules" ".next" /XF ".env.local" ".env"
```
> Nota: robocopy retorna exit code 1 quando copia com sucesso — isso é normal, não é erro.

- [ ] **Step 2: Verificar que os arquivos foram copiados**

```powershell
Get-ChildItem "C:\Users\Andreia Ferreira\cso-corretor\src"
```
Esperado: listar as pastas `app`, `components`, `lib`.

- [ ] **Step 3: Inicializar repositório git**

```powershell
cd "C:\Users\Andreia Ferreira\cso-corretor"
git init
git add .
git commit -m "init: clone from crm-seguros A2 Prime"
```

- [ ] **Step 4: Instalar dependências**

```powershell
npm install
```
Esperado: criação da pasta `node_modules` sem erros.

---

## Task 2: Atualizar cores globais

**Files:**
- Modificar: `src/app/globals.css`
- Modificar: `public/manifest.json`

- [ ] **Step 1: Atualizar globals.css**

Substituir o conteúdo completo de `src/app/globals.css`:
```css
@import "tailwindcss";

:root {
  --background: #E8DDD0;
  --foreground: #1a1a1a;
  --brand-brown: #7B5B3A;
  --brand-purple: #2d1f4e;
  --card-border: #d9cfc4;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
  background: var(--background);
  color: var(--foreground);
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}
```

- [ ] **Step 2: Atualizar manifest.json**

Substituir o conteúdo completo de `public/manifest.json`:
```json
{
  "name": "CSO — Corretor de Seguros Organizado",
  "short_name": "CSO",
  "description": "Sistema de organização para corretores de seguros",
  "start_url": "/dashboard",
  "display": "standalone",
  "background_color": "#7B5B3A",
  "theme_color": "#7B5B3A",
  "orientation": "portrait-primary",
  "icons": [
    {
      "src": "/logo-cso.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/logo-cso.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

- [ ] **Step 3: Commit**

```powershell
git add src/app/globals.css public/manifest.json
git commit -m "style: atualiza cores e manifest para identidade CSO"
```

---

## Task 3: Atualizar Sidebar

**Files:**
- Modificar: `src/components/Sidebar.tsx`

- [ ] **Step 1: Trocar cores inline, logo, labels e rodapé**

Aplicar as seguintes trocas em `src/components/Sidebar.tsx`:

**Linha ~27** (nav admin) e **~38** (nav vendedor) — trocar href e label:
```tsx
{ href: '/cso-academy',  label: 'CSO Academy',  icon: GraduationCap  },
```
(substitui as duas ocorrências de `{ href: '/prime-academy', label: 'Prime Academy', ... }`)

**Linhas ~83 e ~97** — cor do botão mobile e fundo do aside:
```tsx
style={{ backgroundColor: '#7B5B3A' }}
```
(substitui as duas ocorrências de `backgroundColor: '#2d1f4e'`)

**Linhas ~99-106** — logo:
```tsx
<Image
  src="/logo-cso.png"
  alt="CSO"
  width={48}
  height={48}
  className="object-cover rounded-full"
  style={{ width: '48px', height: '48px' }}
  priority
/>
```

**Linha ~120** — fundo item ativo:
```tsx
backgroundColor: active ? 'rgba(45,31,78,0.12)' : 'transparent',
```
(substitui `rgba(184,154,106,0.12)`)

**Linha ~121** — borda item ativo:
```tsx
borderLeft: active ? '3px solid #2d1f4e' : '3px solid transparent',
```
(substitui `3px solid #b89a6a`)

**Linhas ~129-132** — badge da agenda:
```tsx
<span className="text-xs font-bold px-1.5 py-0.5 rounded-full min-w-[20px] text-center"
  style={{ backgroundColor: '#2d1f4e', color: '#E8DDD0' }}>
  {agendaHoje}
</span>
```

**Linhas ~157-159** — avatar do usuário:
```tsx
style={{ backgroundColor: '#2d1f4e', color: '#E8DDD0' }}
```
(substitui `backgroundColor: '#b89a6a', color: '#2d1f4e'`)

**Linha ~166** — texto do rodapé:
```tsx
{perfil === 'admin' ? 'Admin · CSO' : 'Vendedor'}
```
(substitui `'Admin · A2 Prime'`)

- [ ] **Step 2: Commit**

```powershell
git add src/components/Sidebar.tsx
git commit -m "style: atualiza Sidebar para identidade visual CSO"
```

---

## Task 4: Atualizar layout.tsx e página de login

**Files:**
- Modificar: `src/app/layout.tsx`
- Modificar: `src/app/login/page.tsx`

- [ ] **Step 1: Atualizar metadata em layout.tsx**

Substituir o bloco `export const metadata` em `src/app/layout.tsx`:
```tsx
export const metadata: Metadata = {
  title: "CSO | Corretor de Seguros Organizado",
  description: "Sistema de organização para corretores de seguros",
  manifest: "/manifest.json",
  icons: {
    icon: "/logo-cso.png",
    apple: "/logo-cso.png",
  },
};
```

- [ ] **Step 2: Atualizar logo na página de login**

Em `src/app/login/page.tsx`, trocar:
```tsx
src="/logo-a2prime.png"
alt="A2 Prime"
```
por:
```tsx
src="/logo-cso.png"
alt="CSO"
```

- [ ] **Step 3: Commit**

```powershell
git add src/app/layout.tsx src/app/login/page.tsx
git commit -m "style: atualiza metadata e logo da página de login para CSO"
```

---

## Task 5: Renomear rota prime-academy → cso-academy

**Files:**
- Renomear: `src/app/(protected)/prime-academy/` → `src/app/(protected)/cso-academy/`
- Modificar: `src/app/(protected)/cso-academy/page.tsx`
- Modificar: `src/app/(protected)/cso-academy/mpa/page.tsx`
- Modificar: `src/app/(protected)/cso-academy/whatsapp-prime/page.tsx`
- Modificar: `src/components/MpaClient.tsx`
- Modificar: `src/components/WhatsappPrimeClient.tsx`

- [ ] **Step 1: Renomear pasta**

```powershell
Rename-Item "src\app\(protected)\prime-academy" "cso-academy"
```

- [ ] **Step 2: Atualizar hrefs internos em cso-academy/page.tsx**

No arquivo `src/app/(protected)/cso-academy/page.tsx`, substituir todas as ocorrências de `/prime-academy` por `/cso-academy`, e o título "Prime Academy" por "CSO Academy":

```tsx
// Linha com href do MPA:
href: '/cso-academy/mpa',

// Linha com href do whatsapp:
href: '/cso-academy/whatsapp-prime',

// Título da página:
<h1 className="text-2xl font-bold text-gray-900">CSO Academy</h1>

// Descrição:
Central de conhecimento, treinamento e consulta operacional.
```

- [ ] **Step 3: Atualizar breadcrumb em cso-academy/mpa/page.tsx**

No arquivo `src/app/(protected)/cso-academy/mpa/page.tsx`, substituir:
```tsx
// href do breadcrumb:
href="/cso-academy"

// texto do breadcrumb:
CSO Academy
```

- [ ] **Step 4: Atualizar breadcrumb em cso-academy/whatsapp-prime/page.tsx**

No arquivo `src/app/(protected)/cso-academy/whatsapp-prime/page.tsx`, substituir:
```tsx
href="/cso-academy"

// texto:
CSO Academy
```

- [ ] **Step 5: Corrigir import em MpaClient.tsx**

Em `src/components/MpaClient.tsx` linha 5, substituir:
```tsx
import type { Script, Categoria } from '@/app/(protected)/cso-academy/mpa/scripts'
```

- [ ] **Step 6: Corrigir import em WhatsappPrimeClient.tsx**

Em `src/components/WhatsappPrimeClient.tsx` linha 5, substituir:
```tsx
import type { Licao } from '@/app/(protected)/cso-academy/whatsapp-prime/licoes'
```

- [ ] **Step 7: Commit**

```powershell
git add src/app/`(protected`)/cso-academy src/components/MpaClient.tsx src/components/WhatsappPrimeClient.tsx
git commit -m "feat: renomeia rota prime-academy para cso-academy"
```

---

## Task 6: Atualizar referências "A2 Prime" nos componentes

**Files:**
- Modificar: `src/components/ResultadoTab.tsx`
- Modificar: `src/components/FinanceiroClient.tsx`
- Modificar: `src/components/ProducaoTab.tsx`
- Modificar: `src/components/ClienteExportModal.tsx`

- [ ] **Step 1: ResultadoTab.tsx — cabeçalho do relatório**

Em `src/components/ResultadoTab.tsx` linha ~235:
```tsx
<h2 className="text-xl font-bold" style={{ color: '#2d1f4e' }}>Resultado — CSO Corretor de Seguros</h2>
```

- [ ] **Step 2: FinanceiroClient.tsx — cabeçalho**

Em `src/components/FinanceiroClient.tsx` linha ~49:
```tsx
CSO Corretor de Seguros
```

- [ ] **Step 3: ProducaoTab.tsx — cabeçalho PDF**

Em `src/components/ProducaoTab.tsx` linha ~163:
```tsx
doc.text('Produção — CSO Corretor de Seguros', 14, 18)
```

- [ ] **Step 4: ClienteExportModal.tsx — cabeçalho PDF**

Em `src/components/ClienteExportModal.tsx` linha ~61:
```tsx
doc.text('Carteira de Clientes — CSO Corretor de Seguros', 14, 16)
```

- [ ] **Step 5: Commit**

```powershell
git add src/components/ResultadoTab.tsx src/components/FinanceiroClient.tsx src/components/ProducaoTab.tsx src/components/ClienteExportModal.tsx
git commit -m "style: substitui referencias A2 Prime por CSO nos relatorios"
```

---

## Task 7: Substituir logo

> **Pré-requisito:** Andreia precisa exportar a logo do Canva com **fundo transparente** e salvar como `logo-cso.png`.

**Files:**
- Substituir: `public/logo-cso.png` (novo arquivo da Andreia)
- Manter (ou deletar): `public/logo-a2prime.png`

- [ ] **Step 1: Copiar logo CSO para a pasta public**

Andreia exporta a logo do Canva como PNG com fundo transparente.

Copiar o arquivo para:
```
C:\Users\Andreia Ferreira\cso-corretor\public\logo-cso.png
```

- [ ] **Step 2: Verificar que a logo aparece no app**

```powershell
npm run dev
```
Abrir `http://localhost:3000/login` no navegador e verificar que a logo CSO aparece no login e no topo do Sidebar.

- [ ] **Step 3: Commit**

```powershell
git add public/logo-cso.png
git commit -m "style: adiciona logo CSO"
```

---

## Task 8: Configurar variáveis de ambiente

> **Pré-requisito:** Andreia cria novo projeto no Supabase e fornece URL + chaves.

**Files:**
- Criar: `.env.local`

- [ ] **Step 1: Criar .env.local com as chaves do novo projeto Supabase**

Criar o arquivo `.env.local` na raiz do projeto:
```env
NEXT_PUBLIC_SUPABASE_URL=<URL do projeto CSO no Supabase>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key do projeto CSO>
SUPABASE_SERVICE_ROLE_KEY=<service_role key do projeto CSO>
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

- [ ] **Step 2: Verificar conexão com Supabase**

```powershell
npm run dev
```
Abrir `http://localhost:3000/login` e tentar fazer login. Se a tela de login carregar sem erros de conexão, o Supabase está configurado.

- [ ] **Step 3: Aplicar migrations no novo banco**

Com o Supabase CLI configurado apontando para o novo projeto:
```powershell
npx supabase db push
```
Ou aplicar o arquivo `supabase/migrations/_deploy_completo.sql` manualmente pelo dashboard do Supabase.

---

## Task 9: Verificar build final e commitar

- [ ] **Step 1: Rodar build de produção**

```powershell
npm run build
```
Esperado: build finaliza sem erros TypeScript ou de compilação.

- [ ] **Step 2: Corrigir erros se houver**

Se aparecerem erros de TypeScript, corrigi-los antes de continuar.

- [ ] **Step 3: Commit final**

```powershell
git add -A
git commit -m "chore: build verificado — CSO pronto para deploy"
```

---

## Pós-implementação: Deploy no EasyPanel

Após tudo funcionando localmente:
1. Criar repositório GitHub para o `cso-corretor`
2. Fazer push do código: `git remote add origin <URL> && git push -u origin master`
3. No EasyPanel: criar novo app, apontar para o repositório GitHub do CSO
4. Configurar variáveis de ambiente no EasyPanel (as mesmas do `.env.local`, com a URL de produção em `NEXT_PUBLIC_APP_URL`)

> A Andreia vai receber instrução passo a passo para a parte de infraestrutura.
